server:
  host: "127.0.0.1"
  port: 8000

backends:
  mlx:
    url: "http://127.0.0.1:8001"
  llama:
    url: "http://127.0.0.1:8002"
    model: "models/model.gguf"

# Auth and model are configured via .env (see install.sh).
# Do not store API keys in this file.
